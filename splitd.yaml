sdk:
  apikey: "<SPLIT_SERVER_SIDE_SDK_KEY>"
  impressions:
    refreshRateSeconds: 5
    mode: "debug"
  events:
    refreshRateSeconds: 5

link:
  type: "unix-stream"
  address: "./splitd.sock"
  serialization: "msgpack"

logging:
  level: info
